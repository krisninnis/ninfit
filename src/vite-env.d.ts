/// <reference types="vite/client" />

/**
 * Injected by Vite from package.json (see `define` in vite.config.ts), so the
 * version stamped into a backup always matches the app that wrote it.
 */
declare const __APP_VERSION__: string;
